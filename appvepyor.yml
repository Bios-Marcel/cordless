clone_folder: c:\gopath\src\github.com\Bios-Marcel\cordless

environment:
  GOPATH: c:\gopath

stack: go 1.10

artifacts:
  - path: cordless.exe
    name: cordless.exe

build_script:
  - go get -v -d ./...
  - go build -o cordless.exe

test_script:
  - go vet ./...
  - go test ./...